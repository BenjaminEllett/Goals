{
  "name": "goals-web-site",
  "version": "0.1.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "pwsh -NoProfile -File ./Build/Build.ps1 -Release -Build -CleanBuild",
    "start": "pwsh -NoProfile -File ./Build/Build.ps1 -Debug -BuildAndServeWebSite -CleanBuild",
    "test": "echo \"TODO - Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "esbuild": "0.24.0",
    "sass": "^1.79.4",
    "typescript": "^5.6.3"
  }
}
